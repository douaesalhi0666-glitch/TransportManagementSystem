@model IEnumerable<TransportManagementSystem.Models.Trajectory>

@{
    ViewData["Title"] = "Trajets";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Gestion des Trajets</h1>

<p>
    <a href="/Trajectories/Create" class="btn btn-primary">Ajouter un trajet</a>
</p>

<div class="row mb-3">
    <div class="col-md-4">
        <input type="text" id="searchInput" class="form-control" placeholder="Rechercher par ID, nom ou code" />
    </div>
    <div class="col-md-3">
        <select id="statusFilter" class="form-select">
            <option value="">Tous les statuts</option>
            <option value="Active">Actif</option>
            <option value="Inactive">Inactif</option>
        </select>
    </div>
    <div class="col-md-3">
        <select id="assignedFilter" class="form-select">
            <option value="">Tous</option>
            <option value="assigned">Assigné à un bus</option>
            <option value="unassigned">Non assigné</option>
        </select>
    </div>
    <div class="col-md-2">
        <button id="resetFilter" class="btn btn-secondary">Réinitialiser</button>
    </div>
</div>

<table class="table table-striped" id="trajectoriesTable">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Code</th>
            <th>Description</th>
            <th>Distance (km)</th>
            <th>Durée (min)</th>
            <th>Statut</th>
            <th>Assigné à un bus ?</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @{
            var assignedIds = ViewBag.AssignedTrajectoryIds as HashSet<int> ?? new HashSet<int>();
        }
        @foreach (var item in Model)
        {
            bool isAssigned = assignedIds.Contains(item.Trajectory_Id);
            <tr data-id="@item.Trajectory_Id"
                data-name="@item.Trajectory_Name"
                data-code="@item.Trajectory_Code"
                data-status="@item.Trajectory_Status">
                <td>@item.Trajectory_Id</td>
                <td>@item.Trajectory_Name</td>
                <td>@item.Trajectory_Code</td>
                <td>@item.Trajectory_Description</td>
                <td>@item.Trajectory_DistanceKm</td>
                <td>@item.Trajectory_EstimatedDurationMinutes</td>
                <td>
                    @if (item.Trajectory_Status == "Active")
                    {
                        <span class="badge bg-success">Actif</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Inactif</span>
                    }
                </td>
                <td>
                    @if (isAssigned)
                    {
                        <span class="badge bg-primary">Oui</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Non</span>
                    }
                </td>
                <td>
                    <a href="/Trajectories/Edit/@item.Trajectory_Id" class="btn btn-sm btn-warning">Modifier</a>
                    <form asp-action="Delete" asp-route-id="@item.Trajectory_Id" method="post" style="display:inline;">
                        <input type="submit" value="Supprimer" class="btn btn-sm btn-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer ce trajet ?');" />
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="/Dashboard/AdminDashboard" class="btn btn-secondary">Retour au Dashboard</a>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function() {
        function filterTable() {
        var searchText = $('#searchInput').val().toLowerCase().trim();
        var status = $('#statusFilter').val();
        var assignedFilter = $('#assignedFilter').val();

        $('#trajectoriesTable tbody tr').each(function() {
        var $row = $(this);
        var id = $row.data('id').toString();
        var name = $row.data('name').toString().toLowerCase();
        var code = $row.data('code').toString().toLowerCase();
        var rowStatus = $row.data('status');
                    
        // Récupérer le texte de la colonne "Assigné à un bus ?" (le badge)
        var assignedText = $row.find('td:nth-child(8)').text().trim(); // "Oui" ou "Non"
                    
        // Filtre recherche
        var matchSearch = (searchText === '' ||
        id.indexOf(searchText) !== -1 ||
        name.indexOf(searchText) !== -1 ||
        code.indexOf(searchText) !== -1);
                    
        // Filtre statut
        var matchStatus = (status === '' || rowStatus === status);
                    
        // Filtre assignation : on compare le texte "Oui" ou "Non"
        var matchAssigned = true;
        if (assignedFilter === 'assigned') {
        matchAssigned = (assignedText === 'Oui');
        } else if (assignedFilter === 'unassigned') {
        matchAssigned = (assignedText === 'Non');
        }

        if (matchSearch && matchStatus && matchAssigned) {
        $row.show();
        } else {
        $row.hide();
        }
        });
        }

        $('#searchInput').on('keyup', filterTable);
        $('#statusFilter').on('change', filterTable);
        $('#assignedFilter').on('change', filterTable);
        $('#resetFilter').click(function() {
        $('#searchInput').val('');
        $('#statusFilter').val('');
        $('#assignedFilter').val('');
        filterTable();
        });

        filterTable();
        });
    </script>
}