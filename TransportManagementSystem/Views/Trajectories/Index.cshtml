@model IEnumerable<TransportManagementSystem.Models.Trajectory>

@{
    ViewData["Title"] = "Trajets";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    :root {
        --navy: #0f1c2e;
        --navy-mid: #162436;
        --amber: #f5a623;
        --amber-glow: rgba(245, 166, 35, 0.1);
        --text-muted: #64748b;
    }

    .page-header {
        margin-bottom: 1.5rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid var(--amber-glow);
    }

        .page-header h1 {
            font-family: 'Sora', sans-serif;
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0;
        }

        .page-header p {
            color: var(--text-muted);
            font-size: 0.85rem;
            margin: 4px 0 0 0;
        }

    .btn-primary-custom {
        background: linear-gradient(135deg, #1d4ed8, #3b82f6);
        border: none;
        border-radius: 10px;
        padding: 8px 18px;
        font-weight: 600;
        font-size: 0.85rem;
        transition: transform 0.2s, box-shadow 0.2s;
        color: white;
        text-decoration: none;
        display: inline-block;
    }

        .btn-primary-custom:hover {
            background: linear-gradient(135deg, #1e40af, #2563eb);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
            color: white;
            text-decoration: none;
        }

    .filter-bar {
        background: linear-gradient(135deg, #f8fafc, #ffffff);
        padding: 1.25rem;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        border: 1px solid #e2e8f0;
    }

    .table-container {
        max-height: calc(100vh - 280px);
        overflow-y: auto;
        border-radius: 12px;
        border: 1px solid #e2e8f0;
        position: relative;
    }

    .trajectories-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 0.8rem;
        margin-bottom: 0;
    }

        .trajectories-table thead {
            position: sticky;
            top: 0;
            background: #f8fafc;
            color: var(--navy);
            z-index: 10;
            border-bottom: 2px solid #e2e8f0;
        }

            .trajectories-table thead th {
                padding: 14px 12px;
                font-weight: 700;
                font-size: 0.8rem;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                white-space: nowrap;
                color: var(--navy);
            }

        .trajectories-table tbody td {
            padding: 12px;
            border-bottom: 1px solid #e2e8f0;
            white-space: nowrap;
        }

        .trajectories-table tbody tr:hover {
            background-color: var(--amber-glow);
        }

    .badge-active {
        background: linear-gradient(135deg, #10b981, #059669);
        color: white;
        padding: 4px 10px;
        border-radius: 20px;
        font-size: 0.7rem;
        font-weight: 600;
        display: inline-block;
    }

    .badge-inactive {
        background: linear-gradient(135deg, #ef4444, #dc2626);
        color: white;
        padding: 4px 10px;
        border-radius: 20px;
        font-size: 0.7rem;
        font-weight: 600;
        display: inline-block;
    }

    .btn-sm-custom {
        padding: 5px 12px;
        font-size: 0.7rem;
        border-radius: 8px;
        transition: transform 0.15s, box-shadow 0.15s;
        border: none;
        cursor: pointer;
    }

        .btn-sm-custom:hover {
            transform: translateY(-1px);
        }

    .btn-info-custom {
        background: linear-gradient(135deg, #3b82f6, #2563eb);
        color: white;
    }

        .btn-info-custom:hover {
            box-shadow: 0 2px 6px rgba(59, 130, 246, 0.4);
        }

    .btn-warning-custom {
        background: linear-gradient(135deg, #f59e0b, #d97706);
        color: white;
    }

        .btn-warning-custom:hover {
            box-shadow: 0 2px 6px rgba(245, 158, 11, 0.4);
        }

    .btn-danger-custom {
        background: linear-gradient(135deg, #ef4444, #dc2626);
        color: white;
    }

        .btn-danger-custom:hover {
            box-shadow: 0 2px 6px rgba(239, 68, 68, 0.4);
        }

    .btn-secondary-custom {
        background: #64748b;
        border: none;
        border-radius: 10px;
        padding: 8px 18px;
        font-weight: 600;
        font-size: 0.85rem;
        transition: transform 0.15s, background 0.15s;
        color: white;
        text-decoration: none;
        display: inline-block;
    }

        .btn-secondary-custom:hover {
            background: #475569;
            transform: translateY(-1px);
            color: white;
            text-decoration: none;
        }

    .export-buttons {
        display: flex;
        gap: 10px;
        margin-left: auto;
    }

    .btn-export-excel {
        background: linear-gradient(135deg, #1e7e34, #28a745);
        color: white;
        border: none;
        border-radius: 10px;
        padding: 8px 18px;
        font-weight: 600;
        font-size: 0.85rem;
        transition: transform 0.2s, box-shadow 0.2s;
        cursor: pointer;
    }

        .btn-export-excel:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 14px rgba(30, 126, 52, 0.35);
        }

        .btn-export-excel:active {
            transform: translateY(0px);
        }

    .btn-export-pdf {
        background: linear-gradient(135deg, #c82333, #dc3545);
        color: white;
        border: none;
        border-radius: 10px;
        padding: 8px 18px;
        font-weight: 600;
        font-size: 0.85rem;
        transition: transform 0.2s, box-shadow 0.2s;
        cursor: pointer;
    }

        .btn-export-pdf:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 14px rgba(200, 35, 51, 0.35);
        }

        .btn-export-pdf:active {
            transform: translateY(0px);
        }

    .action-buttons {
        display: flex;
        gap: 6px;
        flex-wrap: wrap;
    }

    .search-input:focus, .form-select:focus {
        border-color: var(--amber);
        box-shadow: 0 0 0 3px rgba(245, 166, 35, 0.15);
        outline: none;
    }
</style>

<div class="page-header d-flex justify-content-between align-items-center flex-wrap gap-3">
    <div>
        <h1><i class="fas fa-route me-2" style="color: var(--amber);"></i>Gestion des Trajets</h1>
        <p>Gérez les itinéraires et leurs assignations</p>
    </div>
    <div>
        <a href="/Trajectories/Create" class="btn btn-primary-custom">
            <i class="fas fa-plus me-2"></i>Ajouter un trajet
        </a>
    </div>
</div>

<div class="filter-bar">
    <div class="row g-3 align-items-center">
        <div class="col-md-4">
            <input type="text" id="searchInput" class="form-control search-input" placeholder="Rechercher par ID, nom ou code" />
        </div>
        <div class="col-md-3">
            <select id="statusFilter" class="form-select">
                <option value="">Tous les statuts</option>
                <option value="Active">Actif</option>
                <option value="Inactive">Inactif</option>
            </select>
        </div>
        <div class="col-md-2">
            <button id="resetFilter" class="btn btn-secondary-custom w-100">
                <i class="fas fa-undo-alt me-1"></i>Réinitialiser
            </button>
        </div>
        <div class="col-md-3">
            <div class="export-buttons">
                <button id="exportExcelBtn" class="btn-export-excel"><i class="fas fa-file-excel me-2"></i>Excel</button>
                <button id="exportPdfBtn" class="btn-export-pdf"><i class="fas fa-file-pdf me-2"></i>PDF</button>
            </div>
        </div>
    </div>
</div>

<div class="table-container">
    <table class="trajectories-table" id="trajectoriesTable">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Code</th>
                <th>Description</th>
                <th>Distance (km)</th>
                <th>Durée (min)</th>
                <th>Statut</th>
                <th>Personnels</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr data-id="@item.Trajectory_Id" data-name="@item.Trajectory_Name" data-code="@item.Trajectory_Code" data-status="@item.Trajectory_Status">
                    <td>@item.Trajectory_Id</td>
                    <td><strong>@item.Trajectory_Name</strong></td>
                    <td><code>@item.Trajectory_Code</code></td>
                    <td>@(item.Trajectory_Description ?? "-")</td>
                    <td>@(item.Trajectory_DistanceKm?.ToString("F2") ?? "-")</td>
                    <td>@(item.Trajectory_EstimatedDurationMinutes ?? 0)</td>
                    <td>
                        @if (item.Trajectory_Status == "Active")
                        {
                            <span class="badge-active"><i class="fas fa-check-circle me-1"></i>Actif</span>
                        }
                        else
                        {
                            <span class="badge-inactive"><i class="fas fa-times-circle me-1"></i>Inactif</span>
                        }
                    </td>
                    <td>
                        <button type="button" class="btn-sm-custom btn-info-custom" onclick="showPersonnel(@item.Trajectory_Id, '@item.Trajectory_Name')">
                            <i class="fas fa-users"></i> Voir
                        </button>
                    </td>
                    <td class="action-buttons">
                        <button type="button" class="btn-sm-custom btn-info-custom details-btn"
                                data-id="@item.Trajectory_Id"
                                data-name="@item.Trajectory_Name"
                                data-code="@item.Trajectory_Code"
                                data-description="@item.Trajectory_Description"
                                data-distance="@item.Trajectory_DistanceKm"
                                data-duration="@item.Trajectory_EstimatedDurationMinutes"
                                data-status="@item.Trajectory_Status"
                                data-startlat="@item.Trajectory_StartLatitude"
                                data-startlng="@item.Trajectory_StartLongitude"
                                data-endlat="@item.Trajectory_EndLatitude"
                                data-endlng="@item.Trajectory_EndLongitude">
                            <i class="fas fa-eye"></i> Détails
                        </button>
                        <button type="button" class="btn-sm-custom btn-warning-custom edit-btn" data-id="@item.Trajectory_Id">
                            <i class="fas fa-pen"></i> Modifier
                        </button>
                        <form asp-action="Delete" asp-route-id="@item.Trajectory_Id" method="post" style="display:inline;" onsubmit="return confirm('Supprimer ce trajet ?');">
                            <button type="submit" class="btn-sm-custom btn-danger-custom">
                                <i class="fas fa-trash"></i> Supprimer
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div class="mt-4">
    <a href="/Dashboard/AdminDashboard" class="btn btn-secondary-custom">
        <i class="fas fa-arrow-left me-2"></i>Retour au Dashboard
    </a>
</div>

<!-- Personnel Modal -->
<div class="modal fade" id="personnelModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header" style="background: linear-gradient(135deg, #0f1c2e, #162436); color: white;">
                <h5 class="modal-title"><i class="fas fa-users me-2"></i>Personnels du trajet</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div id="personnelModalContent">Chargement...</div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header" style="background: linear-gradient(135deg, #0f1c2e, #162436); color: white;">
                <h5 class="modal-title"><i class="fas fa-pen me-2"></i>Modifier le trajet</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="editForm">
                    <input type="hidden" id="edit_Trajectory_Id" />
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label-custom">Nom</label>
                            <input type="text" id="edit_Name" class="form-control" required />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-custom">Code</label>
                            <input type="text" id="edit_Code" class="form-control" required />
                        </div>
                    </div>
                    <div class="mt-2">
                        <label class="form-label-custom">Description</label>
                        <textarea id="edit_Description" class="form-control" rows="2"></textarea>
                    </div>
                    <div class="row g-3 mt-2">
                        <div class="col-md-6">
                            <label class="form-label-custom">Distance (km)</label>
                            <input type="number" step="0.01" id="edit_Distance" class="form-control" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-custom">Durée (min)</label>
                            <input type="number" id="edit_Duration" class="form-control" />
                        </div>
                    </div>
                    <h5 class="mt-3" style="color: var(--amber);"><i class="fas fa-map-marker-alt me-2"></i>Départ</h5>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label-custom">Latitude départ</label>
                            <input type="number" step="any" id="edit_StartLat" class="form-control" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-custom">Longitude départ</label>
                            <input type="number" step="any" id="edit_StartLng" class="form-control" />
                        </div>
                    </div>
                    <h5 class="mt-3" style="color: var(--amber);"><i class="fas fa-flag-checkered me-2"></i>Arrivée</h5>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label-custom">Latitude arrivée</label>
                            <input type="number" step="any" id="edit_EndLat" class="form-control" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-custom">Longitude arrivée</label>
                            <input type="number" step="any" id="edit_EndLng" class="form-control" />
                        </div>
                    </div>
                    <div class="row g-3 mt-2">
                        <div class="col-md-6">
                            <label class="form-label-custom">Statut</label>
                            <select id="edit_Status" class="form-select">
                                <option value="Active">Actif</option>
                                <option value="Inactive">Inactif</option>
                            </select>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                <button type="button" id="saveEditBtn" class="btn btn-primary-custom">Enregistrer</button>
            </div>
        </div>
    </div>
</div>

<!-- Details Modal -->
<div class="modal fade" id="detailsModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header" style="background: linear-gradient(135deg, #0f1c2e, #162436); color: white;">
                <h5 class="modal-title"><i class="fas fa-info-circle me-2"></i>Détails du trajet</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div id="detailsContent" class="row">Chargement...</div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.sheetjs.com/xlsx-0.20.2/package/dist/xlsx.full.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.29/jspdf.plugin.autotable.min.js"></script>

    <script>
        $(document).ready(function() {
        function filterTable() {
        var search = $('#searchInput').val().toLowerCase();
        var status = $('#statusFilter').val();
        $('#trajectoriesTable tbody tr').each(function() {
        var $row = $(this);
        var id = $row.data('id').toString();
        var name = $row.data('name').toString().toLowerCase();
        var code = $row.data('code').toString().toLowerCase();
        var rowStatus = $row.data('status');
        var matchSearch = (search === '' || id.indexOf(search) !== -1 || name.indexOf(search) !== -1 || code.indexOf(search) !== -1);
        var matchStatus = (status === '' || rowStatus === status);
        $row.toggle(matchSearch && matchStatus);
        });
        }

        $('#searchInput').on('keyup', filterTable);
        $('#statusFilter').on('change', filterTable);
        $('#resetFilter').click(function() { $('#searchInput').val(''); $('#statusFilter').val(''); filterTable(); });

        // Export Excel
        $('#exportExcelBtn').click(function() {
        var data = [], headers = [];
        $('#trajectoriesTable thead th').each(function() { var t = $(this).text().trim(); if (t !== "Actions" && t !== "Personnels") headers.push(t); });
        data.push(headers);
        $('#trajectoriesTable tbody tr:visible').each(function() {
        var row = [];
        $(this).find('td').each(function(idx) { if (idx < 7) row.push($(this).text().trim()); });
        data.push(row);
        });
        var ws = XLSX.utils.aoa_to_sheet(data);
        var wb = XLSX.utils.book_new();
        XLSX.utils.book_append_sheet(wb, ws, "Trajets");
        XLSX.writeFile(wb, "Trajets_Export.xlsx");
        });

        // Export PDF
        $('#exportPdfBtn').click(function() {
        const { jsPDF } = window.jspdf;
        var doc = new jsPDF({ orientation: 'landscape', unit: 'mm', format: 'a4' });
        var columns = [];
        $('#trajectoriesTable thead th').each(function() { var t = $(this).text().trim(); if (t !== "Actions" && t !== "Personnels") columns.push(t); });
        var rows = [];
        $('#trajectoriesTable tbody tr:visible').each(function() {
        var row = [];
        $(this).find('td').each(function(idx) { if (idx < 7) row.push($(this).text().trim()); });
        rows.push(row);
        });
        doc.autoTable({ head: [columns], body: rows, theme: 'striped', headStyles: { fillColor: [15, 28, 46], textColor: 255 }, margin: { top: 20 }, didDrawPage: (data) => { doc.setFontSize(12); doc.text("Liste des Trajets", data.settings.margin.left, 10); } });
        doc.save("Trajets_Export.pdf");
        });

        // Details
        $('.details-btn').click(function() {
        var id = $(this).data('id');
        var name = $(this).data('name');
        var code = $(this).data('code');
        var description = $(this).data('description');
        var distance = $(this).data('distance');
        var duration = $(this).data('duration');
        var status = $(this).data('status');
        var startLat = $(this).data('startlat');
        var startLng = $(this).data('startlng');
        var endLat = $(this).data('endlat');
        var endLng = $(this).data('endlng');
        var html = `<div class="col-md-6">
        <div class="card" style="border-radius: 12px; border: 1px solid #e2e8f0;">
        <div class="card-header" style="background: linear-gradient(135deg, #1d4ed8, #3b82f6); color: white; border-radius: 12px 12px 0 0;">
        <i class="fas fa-info-circle me-2"></i>Informations générales
        </div>
        <div class="card-body">
        <p><strong>ID:</strong> ${id}</p>
        <p><strong>Nom:</strong> ${name}</p>
        <p><strong>Code:</strong> ${code}</p>
        <p><strong>Description:</strong> ${description || '-'}</p>
        <p><strong>Distance:</strong> ${distance || '-'} km</p>
        <p><strong>Durée:</strong> ${duration || '-'} min</p>
        <p><strong>Statut:</strong> ${status === 'Active' ? '<span class="badge-active">Actif</span>' : '<span class="badge-inactive">Inactif</span>'}</p>
        </div>
        </div>
        </div>
        <div class="col-md-6">
        <div class="card" style="border-radius: 12px; border: 1px solid #e2e8f0;">
        <div class="card-header" style="background: linear-gradient(135deg, #059669, #10b981); color: white; border-radius: 12px 12px 0 0;">
        <i class="fas fa-map-marker-alt me-2"></i>Points GPS
        </div>
        <div class="card-body">
        <p><strong><i class="fas fa-play-circle me-1" style="color: #10b981;"></i> Départ:</strong><br/>Lat: ${startLat || '-'}<br/>Lng: ${startLng || '-'}</p>
        <p><strong><i class="fas fa-flag-checkered me-1" style="color: #ef4444;"></i> Arrivée:</strong><br/>Lat: ${endLat || '-'}<br/>Lng: ${endLng || '-'}</p>
        </div>
        </div>
        </div>`;
        $('#detailsContent').html(html);
        $('#detailsModal').modal('show');
        });

        // Edit
        $('.edit-btn').click(function() {
        var id = $(this).data('id');
        $.get('/Trajectories/GetTrajectoryData/' + id, function(data) {
        $('#edit_Trajectory_Id').val(data.trajectory_Id);
        $('#edit_Name').val(data.trajectory_Name);
        $('#edit_Code').val(data.trajectory_Code);
        $('#edit_Description').val(data.trajectory_Description);
        $('#edit_Distance').val(data.trajectory_DistanceKm);
        $('#edit_Duration').val(data.trajectory_EstimatedDurationMinutes);
        $('#edit_StartLat').val(data.trajectory_StartLatitude);
        $('#edit_StartLng').val(data.trajectory_StartLongitude);
        $('#edit_EndLat').val(data.trajectory_EndLatitude);
        $('#edit_EndLng').val(data.trajectory_EndLongitude);
        $('#edit_Status').val(data.trajectory_Status);
        $('#editModal').modal('show');
        }).fail(function() { alert('Erreur'); });
        });

        $('#saveEditBtn').click(function() {
        var formData = {
        Trajectory_Id: $('#edit_Trajectory_Id').val(),
        Trajectory_Name: $('#edit_Name').val(),
        Trajectory_Code: $('#edit_Code').val(),
        Trajectory_Description: $('#edit_Description').val(),
        Trajectory_DistanceKm: $('#edit_Distance').val() === '' ? null : parseFloat($('#edit_Distance').val()),
        Trajectory_EstimatedDurationMinutes: $('#edit_Duration').val() === '' ? null : parseInt($('#edit_Duration').val()),
        Trajectory_StartLatitude: $('#edit_StartLat').val() === '' ? null : parseFloat($('#edit_StartLat').val()),
        Trajectory_StartLongitude: $('#edit_StartLng').val() === '' ? null : parseFloat($('#edit_StartLng').val()),
        Trajectory_EndLatitude: $('#edit_EndLat').val() === '' ? null : parseFloat($('#edit_EndLat').val()),
        Trajectory_EndLongitude: $('#edit_EndLng').val() === '' ? null : parseFloat($('#edit_EndLng').val()),
        Trajectory_Status: $('#edit_Status').val()
        };
        $.ajax({ url: '/Trajectories/UpdateTrajectory', type: 'POST', contentType: 'application/json', data: JSON.stringify(formData), success: function(r) { if (r.success) { alert('✅ ' + r.message); location.reload(); } else alert('❌ ' + r.message); }, error: function() { alert('Erreur'); } });
        });
        });

        function showPersonnel(trajectoryId, trajectoryName) {
        const modalContent = document.getElementById('personnelModalContent');
        modalContent.innerHTML = '<div class="text-center"><div class="spinner-border text-primary"></div><br>Chargement...</div>';
        fetch(`/Trajectories/GetPersonnelByTrajectory/${trajectoryId}`)
        .then(r => r.json())
        .then(data => {
        if (data.personnel && data.personnel.length) {
        let html = `<div class="alert alert-info" style="background: var(--amber-glow); border-left: 4px solid var(--amber);">
        <i class="fas fa-info-circle me-2"></i>
        <strong>Trajet:</strong> ${data.trajectoryName} (${data.trajectoryCode})<br>
        <strong>Nombre:</strong> ${data.personnelCount}
        </div>
        <div class="table-responsive">
        <table class="table table-striped">
        <thead style="background: linear-gradient(135deg, #0f1c2e, #162436); color: white;">
        <tr><th>ID</th><th>Prénom</th><th>Nom</th><th>Genre</th><th>Téléphone</th><th>Email</th><th>Code</th><th>Département</th><th>Adresse</th><th>Bus</th><th>Statut</th></tr>
        </thead>
        <tbody>`;
        data.personnel.forEach(p => {
        html += `<tr>
        <td>${p.personnel_Id}</td>
        <td>${p.personnel_FirstName}</td>
        <td>${p.personnel_LastName}</td>
        <td>${p.personnel_Gender === 'Male' ? 'Homme' : 'Femme'}</td>
        <td>${p.personnel_PhoneNumber || '-'}</td>
        <td>${p.personnel_Email || '-'}</td>
        <td>${p.personnel_EmployeeCode}</td>
        <td>${p.personnel_Department || '-'}</td>
        <td>${p.homeAddress || '-'}</td>
        <td>${p.assignedBusCode}</td>
        <td>${p.personnel_Status === 'Active' ? '<span class="badge-active">Actif</span>' : '<span class="badge-inactive">Inactif</span>'}</td>
        </tr>`;
        });
        html += `</tbody></table></div>`;
        modalContent.innerHTML = html;
        } else {
        modalContent.innerHTML = '<div class="alert alert-warning">Aucun personnel assigné à ce trajet.</div>';
        }
        new bootstrap.Modal(document.getElementById('personnelModal')).show();
        })
        .catch(e => { modalContent.innerHTML = '<div class="alert alert-danger">Erreur de chargement.</div>'; new bootstrap.Modal(document.getElementById('personnelModal')).show(); });
        }
    </script>
}